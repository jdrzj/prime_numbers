# multiplication table of prime numbers
First thing first. Run bundle install:

`$ bundle install`

Run app:

`$ ruby app.rb <table-size>`

example:

`$ ruby app.rb 4`

table-size default is 10

Run tests:

`$ rspec`
